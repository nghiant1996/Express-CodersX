extends ../layouts/common.pug
block content  
  h1 Profile
  
  table.table
    tr
      th Avatar
      th Name
    tr
      td
        img(src= user.avatar width="32")
      td= user.name
  
  a(href="/users/profile/avatar/" + user.id) Update Avatar
    